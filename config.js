var config = {
development: {
    //url to be used in link generation
    //mongodb connection settings
    database: {
        username:   'dbUser',
        password:   'dbUser'
  
    }
}
// other environments

};
module.exports = config;